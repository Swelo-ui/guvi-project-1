name: keep-warm
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping
        run: curl -sS --max-time 15 https://guvi-project-1-wefr.onrender.com/health > /dev/null
